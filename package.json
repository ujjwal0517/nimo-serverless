{
  "name": "nimo-backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc && sam build",
    "deploy": "aws cloudformation delete-stack --stack-name NimoBackendStack --region ap-southeast-2 || true && sleep 10 && sam deploy --config-file samconfig.toml"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.149",
    "@types/node": "^22.15.3",
    "@types/uuid": "^10.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.799.0",
    "@aws-sdk/client-ses": "^3.798.0",
    "@aws-sdk/lib-dynamodb": "^3.799.0",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1",
    "reflect-metadata": "^0.2.2",
    "uuid": "^11.1.0"
  }
}
